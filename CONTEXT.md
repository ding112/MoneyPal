# 个人财务账本

本项目让人通过自然语言查询和维护复式记账账本，同时保留人在正式写入前的最终决定权。

## Language

**正式账本**：
保存个人真实财务记录、作为财务事实来源的账本。
_Avoid_: 生产账本、真实数据文件

**账本目录**：
一个账本的边界，其中包含主账本、账户声明和按年度组织的交易文件。
_Avoid_: 账本路径、数据目录

**插件工作区**：
用于开发和测试财务工具插件的代码工作区，不保存个人真实财务记录。
_Avoid_: 当前工作区、项目目录

**账本工作区**：
宿主为会话指定或会话所在的工作区；其 `default/` 子目录是该会话唯一正式账本的账本目录。
_Avoid_: 运行目录、账本路径

**MCP 宿主**：
通过 MCP 访问财务工具的宿主应用；服务器以 stdio 子进程方式被其启动。
_Avoid_: MCP 客户端、MCP 通道

**账本会话**：
以一个账本工作区为上下文、因而对应唯一正式账本的 Harness 会话。
_Avoid_: 财务会话、记账会话

**演示账本**：
随项目提供、只用于示例、测试和初次体验的非真实账本。
_Avoid_: 默认账本、测试账本

**验收账本**：
为验证发布候选而构造、覆盖明确会计与故障场景且不包含个人财务事实的合成账本；写入验证只能作用于其临时副本。
_Avoid_: 真实账本、用户账本、生产副本

**主账本**：
一个账本的读取入口，汇集账户声明和各交易文件。
_Avoid_: 主文件、入口文件

**交易文件**：
主账本所汇集的、实际保存交易记录的年度文件。
_Avoid_: 数据文件、流水文件

**财务工具**：
Agent 用来查询或修改账本的受控能力集合，其输入是财务领域参数而不是任意命令。
_Avoid_: 命令行财务工具、Shell 工具

**账本引擎**：
财务流程用来校验和查询正式账本、检查候选交易的领域接口；调用方不感知其解析器、查询语言或进程实现。
_Avoid_: Beancount 客户端、旧账本客户端

**bridge 接缝**：
账本引擎 implementation 内部用于在 TypeScript 与短生命周期 Python 进程间交换版本化 JSON envelope 的私有进程边界；可用于内部代码、测试和工程文档，不进入财务工具公共契约或面向用户的错误。

**MoneyPal 运行时**：
账本引擎执行 Beancount 账本语义所需的兼容 Python、Beancount 与 beanquery 环境，由同一操作系统用户下的 MoneyPal 适配器共享。
_Avoid_: Python 环境、Beancount 环境、插件运行时

**运行时覆盖**：
高级用户明确配置、由 MoneyPal 直接启动并替代托管 MoneyPal 运行时的 Python 解释器。
_Avoid_: 自定义命令、Python 命令、运行时路径

**候选交易**：
已经结构化并可供检查，但尚未获准写入正式账本的交易。
_Avoid_: 临时交易、草稿文本

**交易批次**：
在一次预览和确认中作为不可分割整体处理和写入的一笔或多笔候选交易。
_Avoid_: 批量命令、交易列表

**账户名**：
账户在正式账本中的唯一标识；财务工具的输入和输出以及界面展示均使用同一个精确字符串。
_Avoid_: 底层账户标识、账户显示名、账户别名

**声明账户**：
账本明确允许交易使用的账户名称。
_Avoid_: 已知账户、合法账户

**账户余额**：
一个声明账户在指定日期范围内按币种或计量单位累计的净额。
_Avoid_: 账户金额、当前金额

**金额**：
一个精确十进制数量与商品的组合；不同商品的金额分别保留，不经过浮点数或隐式汇率换算。
_Avoid_: 浮点金额、已换算金额

**写入确认**：
人在查看已通过校验的交易批次后，明确授权将整个批次写入正式账本的决定。
_Avoid_: 审批、二次确认

**写入结果不确定**：
写入已经越过原子发布点，但系统无法证明正式账本最终采用了旧版本还是新版本；再次记账前必须先查询正式账本。
_Avoid_: 写入失败、可以安全重试

**账本维护**：
人在日常记账之外，明确发起的账户结构或账本文件组织调整。
_Avoid_: 特殊操作、直接改文件
