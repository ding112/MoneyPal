# 分别发布 DSH 与 MCP 适配器

DSH 插件与 MCP 服务器面向不同宿主，安装内容和启动接口也不同，因此从同一私有工作区分别组装并发布 `dsh-moneypal` 与 `mcp-moneypal`。财务核心保持一份源码并编译进两个发布包，以少量产物重复换取独立安装和发布，同时避免引入第三个运行时核心包；这取代 ADR-0004 中通过 `dsh-moneypal mcp` 子命令同包交付 MCP 的部分。
