## Design Context

### 用户与任务

依据 CONTEXT.md，产品服务于通过自然语言查询和维护个人复式账本的用户。余额抽屉用于查看资产、负债及账户明细。正式写入由用户决定。

### 语气与视觉方向

本次用户明确要求精简余额抽屉：优先删减多余界面，保留功能和信息层级。沿用已确认的轻量清单与概览／明细双页签；语气简洁、克制，准确说明财务含义。

### 实现约定

沿用现有 React 工厂组件、TypeScript 控制器、命名空间 CSS 与 DSH 语义颜色变量。保留宿主亮暗主题及中英文切换，不新增依赖。浏览器验收使用 ego-browser。

### 设计原则

- 以留白、对齐和字重组织内容，避免卡片嵌套和逐行装饰边框。
- 色彩用于选中、交互和真实异常，不给普通分组添加装饰色。
- 合并重复计数；仅在适用场景显示辅助说明。
- 保留金额精度、币种、负债展示含义、异常提示与数据更新时间。
- 精简装饰不缩小点击区域，不破坏键盘导航与移动端模态行为。
